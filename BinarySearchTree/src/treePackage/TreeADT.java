package treePackage;

import java.util.Iterator;

public interface TreeADT extends Comparable {
    //boolean isEmpty();
    //int size();
   // int height();
    boolean contains(int elem);
   // boolean add(int elem);
    //boolean remove(int elem);
    //Iterator<T> traverse(TreeTraverseType type);
	boolean remove(String elem);
}